import { createAction } from '@ngrx/store';

export const addInFavorites = createAction('[Books Component] Add in favorites');

export const removeFromFavorites = createAction('[Books Component] Remove from favorites');